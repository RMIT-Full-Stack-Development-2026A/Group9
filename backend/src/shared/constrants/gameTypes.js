/**
 * ============================================================================
 * GAME TYPES (The Rulebook Constants)
 * ============================================================================
 * Purpose: This file serves as the "Single Source of Truth" for all fixed 
 * values in TicTacToang. Instead of typing "waiting" or "active" in 10 
 * different files (and risking a typo like "activee"), we define them here.
 * * Key Responsibilities:
 * 1. Status Definitions: Enum-like constants for Game, Room, and Move states.
 * 2. Symbol Management: Defining 'X', 'O', and 'EMPTY'.
 * 3. Configuration: Storing fixed board sizes or turn time limits.
 * * CRITICAL RULE: This file should contain ONLY constants. No logic, 
 * no database calls, and no secret keys. It is safe to share with the frontend!
 */
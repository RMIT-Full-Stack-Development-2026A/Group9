/**
 * ============================================================================
 * GAME TYPES (The Rulebook / Constants)
 * ============================================================================
 * Purpose: This file acts as the "Single Source of Truth" for all strings, 
 * enums, and constant values used across the TicTacToang Game module. 
 * Using constants instead of "hardcoded strings" prevents bugs caused by typos.
 * * Key Responsibilities:
 * 1. Define Game Statuses (so you don't accidentally type 'finish' vs 'finished').
 * 2. Define AI Difficulty levels.
 * 3. Define Player Symbols (X and O).
 * * CRITICAL RULE: This file contains NO logic. It only contains exported 
 * variables. If you change a status name here, it updates across the entire 
 * application automatically.
 */